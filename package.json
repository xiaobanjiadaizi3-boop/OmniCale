{
  "name": "omnicale",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspace @omnicale/core",
    "typecheck": "tsc --build --force",
    "dev": "npm run dev --workspace @omnicale/web",
    "build": "npm run build --workspace @omnicale/web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
